C\_ defines synonyms for some array types that are used frequently:
\type{Encoding} is a synonym for \tt{UByte []}, \type{String} is a synonym
for \tt{Char []}, and \type{Tape} is a synonym for \tt{Ptr (Void) []};
the modifiable twins end with an underscore.
