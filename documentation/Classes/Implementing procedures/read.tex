If data can be successfully read from the input stream \tt{in}
and the resulting value does not exceed \tt{UNSIGNED_MAX}, then the
pointer \tt{this} is returned; otherwise the return value is \tt{NULL}.
