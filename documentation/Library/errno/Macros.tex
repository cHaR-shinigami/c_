\tt{<errno._>} additionally defines the object-like macros \idx{errnum}
and \idx{errnum_}: \tt{errnum_} is same as \tt{errno} and is modifiable,
whereas \tt{errnum} is its non-modifiable variant;
in other words, \tt{errnum_} is an lvalue, but \tt{errnum} is not.
